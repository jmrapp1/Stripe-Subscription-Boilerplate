import Resource from '../Resource';

export default class CreateSubscriptionResource extends Resource {

    /*
    currency: 'usd',
                interval: subInterval,
                product: sub.stripeProductId,
                aggregate_usage: 'last_during_period',
                amount: 1,
                usage_type: 'metered'
     */
    currency = '';
    interval = '';


}
